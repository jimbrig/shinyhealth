% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_response.R
\name{health_response}
\alias{health_response}
\alias{new_health_response}
\alias{validate_health_response}
\alias{is_health_response}
\alias{as_health_response}
\title{Health Check Response}
\usage{
health_response(
  status = "pass",
  ...,
  checks = NULL,
  status_code = 200L,
  timestamp = ts(),
  headers = list(),
  content_type = "application/health+json"
)

new_health_response(
  body = list(),
  status_code = 200L,
  content_type = "application/health+json",
  headers = list()
)

validate_health_response(
  x,
  arg = rlang::caller_arg(x),
  call = rlang::caller_env()
)

is_health_response(x)

as_health_response(x, ...)
}
\arguments{
\item{status}{Character string reported as the body \code{status} field. Default \code{"pass"}.}

\item{...}{Additional named fields merged into the response body at the top level
(e.g. \code{version}, \code{description}, \code{output}).}

\item{checks}{Optional named list of check results, serialized under the body's \code{checks} field.
Elements may be arbitrarily nested lists; their structure is not interpreted.}

\item{status_code}{Integer HTTP status code between \code{100} and \code{599}. Default \code{200L}.}

\item{timestamp}{Character timestamp for the body's \code{timestamp} field. Default \code{\link[=ts]{ts()}}.}

\item{headers}{Named list of HTTP headers merged over the defaults (caller wins).}

\item{content_type}{MIME type for the response. Default \code{"application/health+json"}.}

\item{body}{Named list representing the structured response body (low-level constructor).}

\item{x}{An object to coerce or test.}
}
\value{
\itemize{
\item \code{health_response()}, \code{new_health_response()}, and \code{as_health_response()} return a \code{health_response}
object (a classed \code{\link[shiny:httpResponse]{shiny::httpResponse()}}).
\item \code{validate_health_response()} invisibly returns its input if valid, otherwise throws a
\code{check_error} condition.
\item \code{is_health_response()} returns a logical scalar.
}
}
\description{
Functions for creating and working with \code{health_response} objects, the responses served from
registered healthcheck endpoints:
\itemize{
\item \code{health_response()}: user-facing helper that assembles a response body, applies defaults, validates
inputs, and constructs the object.
\item \code{new_health_response()}: low-level constructor for developers extending the class. Performs no
validation or defaulting beyond serialization.
\item \code{validate_health_response()}: structural validator.
\item \code{is_health_response()}: predicate.
\item \code{as_health_response()}: coercion generic used to normalize healthcheck handler return values.
}

A \code{health_response} \emph{is} a \code{\link[shiny:httpResponse]{shiny::httpResponse()}} (classed \code{c("health_response", "httpResponse")}),
so shiny's internal HTTP machinery serves it like any other response. The structured (pre-serialization)
response body is retained in the \code{body} attribute for printing, inspection, and testing.
}
\details{
\subsection{Status Semantics}{

The HTTP transport status (\code{status_code}) and the body \code{status} field are deliberately independent:
\itemize{
\item Orchestrators and load balancers (Kubernetes, Cloud Run, Azure Container Apps, etc.) interpret only
the \emph{HTTP status code}: codes >= 200 and < 400 indicate success, anything else indicates failure.
\item The body \code{status} string is a convention for humans and tooling. The default value (\code{"pass"}) follows
the IETF health check response format draft (\code{draft-inadarei-api-health-check}, which also suggests
\code{"warn"} and \code{"fail"}), but any value is accepted -- use whatever vocabulary fits your conventions.
}

The defaults compose into a healthy response (\code{status = "pass"}, \code{status_code = 200L}). For an
unhealthy response, \code{503L} (Service Unavailable) is the standard choice, typically paired with
\code{status = "fail"}. How individual \code{checks} aggregate into an overall status is intentionally \emph{not}
decided here -- that policy belongs to the handler that builds the response.
}

\subsection{Headers, Content Type, & Timestamps}{
\itemize{
\item Default headers are \code{Cache-Control: no-store} (RFC 9111) so probe responses are never cached.
The legacy \code{Pragma}/\code{Expires} response headers are deprecated by RFC 9111 and intentionally omitted.
CORS headers are not sent by default (probes are server-to-server); supply them via \code{headers} if a
browser client on another origin needs access.
\item The default content type is \code{application/health+json} per the IETF draft. The \code{+json} structured
syntax suffix (RFC 6839) guarantees any JSON processor handles it; use \code{content_type} to override
(e.g. plain \code{application/json}).
\item Timestamps use RFC 3339 / ISO 8601 format in UTC (see \code{\link[=ts]{ts()}}).
}
}
}
\examples{
# healthy response with defaults
health_response()

# unhealthy response (standard choice: 503 + "fail")
health_response(status = "fail", status_code = 503L)

# custom vocabulary, extra fields, and nested checks
health_response(
  status = "HEALTHY",
  version = "1.0.0",
  checks = list(
    database = list(status = "pass", latency_ms = 2.3),
    api = list(status = "warn", output = "slow response times")
  )
)
}

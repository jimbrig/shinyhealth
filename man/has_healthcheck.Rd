% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_healthcheck.R
\name{has_healthcheck}
\alias{has_healthcheck}
\alias{healthcheck_paths}
\title{Healthcheck Introspection}
\usage{
has_healthcheck(app)

healthcheck_paths(app)
}
\arguments{
\item{app}{A shiny application object, typically created using \code{\link[shiny:shinyApp]{shiny::shinyApp()}}.}
}
\value{
\itemize{
\item \code{has_healthcheck()}: a logical scalar.
\item \code{healthcheck_paths()}: a character vector of registered paths (\code{character(0)} if none).
}
}
\description{
\itemize{
\item \code{has_healthcheck()}: does the app have at least one registered healthcheck endpoint?
\item \code{healthcheck_paths()}: which paths are registered?
}
}
\examples{
app <- shiny::shinyApp(shiny::fluidPage(), function(input, output, session) {})
has_healthcheck(app)

app <- add_healthcheck(app)
has_healthcheck(app)
healthcheck_paths(app)
}

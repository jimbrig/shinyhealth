% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/health_handler.R
\name{health_handler_default}
\alias{health_handler_default}
\title{Default Healthcheck Handler}
\usage{
health_handler_default(req)
}
\arguments{
\item{req}{A shiny request object}
}
\value{
A \code{health_response} object.
}
\description{
The default handler used by \code{\link[=add_healthcheck]{add_healthcheck()}}. Returns a healthy \code{\link[=health_response]{health_response()}}
with no additional fields or checks.

This function also documents the \emph{handler contract}: a healthcheck handler is any function
that accepts the incoming request object (\code{req}) and returns one of:
\itemize{
\item a \code{health_response} (see \code{\link[=health_response]{health_response()}})
\item a plain \code{\link[shiny:httpResponse]{shiny::httpResponse()}} (passed through untouched)
\item a named \code{list}, treated as the complete response body. If it contains a \code{status_code}
field, that value is used as the HTTP status code (and removed from the body); a
\code{timestamp} field is added when absent.
}

Handler return values are normalized via \code{\link[=as_health_response]{as_health_response()}}. If a handler throws an
error, \code{\link[=add_healthcheck]{add_healthcheck()}} converts it into a \code{503} response with \code{status = "fail"} and the
condition message under \code{output} -- the endpoint stays machine-readable even when a check
is broken.
}
\examples{
health_handler_default(req = NULL)
}
\seealso{
\code{\link[=add_healthcheck]{add_healthcheck()}}, \code{\link[=health_response]{health_response()}}
}
